import { Button, Container } from "./Header.styled";

const Header = () => {
  return (
    <Container>
      <h2>Products</h2>
      <Button>+ New Product</Button>
    </Container>
  );
};

export default Header;
